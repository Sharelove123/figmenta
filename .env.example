# === Backend (Django) ===
# Set to 'False' for production
DEBUG=True

# Generate a strong random string for production
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=django-insecure-dev-key-change-in-production

# Comma-separated list of allowed hostnames
# For Render, use your app's domain, e.g., "my-app.onrender.com"
ALLOWED_HOSTS=localhost,127.0.0.1,.onrender.com

# Comma-separated list of allowed CORS origins (Frontend URL)
# For Vercel, add your Vercel deployment URL here
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-app.vercel.app


# === Frontend (Next.js) ===
# The URL of your deployed backend API (Render)
# For local development, use: http://localhost:8000/api
NEXT_PUBLIC_API_URL=http://localhost:8000/api
